import UnsubscribeCard from "@/components/unsubscribe-card";

export default function RemoveSubscriptionPage() {
  return (
    <main className="flex-1 flex h-screen items-center justify-center">
      <UnsubscribeCard />
    </main>
  );
}
